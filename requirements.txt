fastapi==0.95.1       # FastAPI for building the API
uvicorn==0.22.0       # ASGI server for FastAPI
spacy==3.5.0          # NLP library for text processing
pytesseract==0.3.10   # Python wrapper for Tesseract OCR
Pillow==9.2.0         # Image processing library (used with pytesseract)
configparser==5.0.2   # Parsing configuration files (ini, conf)
